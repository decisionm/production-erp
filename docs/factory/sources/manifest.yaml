# Source registry — where the original evidence lives.
#
# This file POINTS at evidence; it never contains it. Statuses:
#   present  — in this repository at path; sha256 pinned (validate.py checks both)
#   missing  — evidence we once read and no longer hold; notes say what is needed
#   external — exists outside the repository; at risk of loss
#   endpoint — a live system, queried fresh, never cached here
#
# Constrained YAML subset — see scripts/factory-knowledge/lib.py.

workbook-product-master:
  name: "Factory workbook, all 103 product rows (extracted)"
  path: "backend/tests/fixtures/product-master-rows.json"
  type: workbook
  status: present
  sha256: "dd7e64100180394f1cd249fed9bc58bc76161c7b7e57d5b5402d925cd5bce60b"
  verified_at: 2026-08-06
  scope: "products, cavities, unit weights, cycle times, carton/tray/pouch specs, pack counts"
  notes: "Canonical tracked copy. backend/storage/app/product-master-rows.json is the same bytes but gitignored — cite the fixture. 18 of 103 rows are pack variants, not products."

paper-reports-workbook:
  name: "Swaashpet paper production + idle reports (ideal and actual)"
  path: "docs/factory-paper-entry/Swaashpet_Paper_Reports_Ideal_and_Actual.xlsx"
  type: workbook
  status: present
  sha256: "61856433fef3bc134eeacbdc7aad42ac14dc49e01dde03b6093e70e36bb1776c"
  verified_at: 2026-08-06
  scope: "handwritten shift pages transcribed; per-shift resin and MB; no packing columns on paper"

tally-consumption-snapshot:
  name: "Tally sync and consumption snapshot workbook"
  path: "docs/factory-paper-entry/Tally_Sync_and_Consumption_Snapshot.xlsx"
  type: workbook
  status: present
  sha256: "629ba6d03e14495d50e7d15c878dfab542907d778dce0f920c3f647f64aa6813"
  verified_at: 2026-08-06
  scope: "consumption cross-check against Tally"

tally-transactions-xml:
  name: "Transactions.xml — 38 real Stock Journals, SWAASHPET 26-27, exported 30 Jul"
  path: "~/Downloads/Transactions.xml"
  type: tally
  status: missing
  notes: "Read on 05 Aug (UTF-16). Ground truth for FC-02/03/04 and the resin/masterbatch decisions. DELETED from Downloads since — ask the factory to re-share, then commit under docs/factory/sources/. Findings preserved in decision records and PR bodies only."

dose-sheet-photos:
  name: "Handwritten pouch and cover count sheets (2 photos, 06 Aug)"
  path: "~/Downloads/WhatsApp Image 2026-08-06 at 12.24.*.jpeg"
  type: physical-process
  status: external
  verified_at: 2026-08-06
  scope: "nos-per-kg counts: pouches 71/56/59; covers 11/25/20/15"
  notes: "AT RISK — same failure mode as Transactions.xml. Owner permission to commit requested (PENDING Q13). The per-piece column on these sheets is rounded and unusable; only the counts are measurements."

live-erp:
  name: "Live ERP instance"
  path: "https://erpdemo.amrtech.in"
  type: live-erp
  status: endpoint
  notes: "Count on live, never on dev — dev fixture counts caused a wrong HRMS call once. Master data changes only via the manual GitHub workflows, dry-run first."

tally-via-agent:
  name: "Tally (factory PC, via sync agent)"
  path: "agent:tally-sync-agent"
  type: tally
  status: endpoint
  notes: "Agent-initiated, one-directional. The ERP never reaches Tally directly. Stock summaries arrive as read-only preview snapshots."

erppro-29jul-rows:
  name: "ERPPro legacy export, 29 Jul rows"
  path: "backend/storage/app/erppro-29jul-rows.json"
  type: workbook
  status: external
  notes: "Untracked (storage is gitignored). Superseded for most purposes by workbook-product-master; kept for cross-checking legacy names."
